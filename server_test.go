package karigo

import (
	"testing"
)

func TestServer(t *testing.T) {
	// srv := &Server{
	// 	Nodes: map[string]*Node{
	// 		"test1": &Node{},
	// 	},
	// }
	// for i := range srv.Nodes {
	// 	go srv.Nodes[i].Run()
	// }

	// tsrv := httptest.NewServer(srv)
	// fmt.Printf("Test server started on %s.\n", strings.TrimPrefix(tsrv.URL, "http://"))

	// tsrv.Close()
	// fmt.Printf("Test server closed.\n")
}
